{
  "name": "dotdo-monorepo",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "description": "Business-as-Code: Events → Models → Billing",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "turbo typecheck",
    "lint": "eslint packages/*/src",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250115.0",
    "eslint": "^9.18.0",
    "turbo": "^2.4.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "stripe": "^17.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
