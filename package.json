{
  "name": "dotdo-v2",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "build:docs": "cd app && npm run build",
    "deploy:docs": "wrangler deploy --config wrangler.static.jsonc"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250109.0",
    "hono": "^4.6.0",
    "typescript": "^5.7.3",
    "vitest": "3.0.2",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^6.1.3",
    "zod": "^3.24.0"
  }
}
