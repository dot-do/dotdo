/**
 * Shared constants for Cap'n Web RPC
 */

/** Default request timeout in milliseconds */
export const DEFAULT_REQUEST_TIMEOUT = 30000

/** Default retry backoff in milliseconds */
export const DEFAULT_RETRY_BACKOFF = 10

/** Slow URL simulation delay for testing */
export const SLOW_DELAY_MS = 500

/** Short polling interval for abort signal checking */
export const ABORT_CHECK_INTERVAL_MS = 5
