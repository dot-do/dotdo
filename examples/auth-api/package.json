{
  "name": "auth-api-example",
  "version": "1.0.0",
  "description": "DOFull with WorkOS authentication via oauth.do",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
