{
  "name": "compat-clerk",
  "version": "1.0.0",
  "private": true,
  "description": "Clerk Backend API compatibility layer on Durable Objects - users, sessions, organizations, invitations, JWT templates, webhooks",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "clerk",
    "auth",
    "authentication",
    "jwt",
    "oauth",
    "organizations",
    "mfa",
    "totp",
    "webhooks",
    "durable-objects",
    "cloudflare-workers"
  ],
  "dependencies": {
    "dotdo": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.58.0"
  }
}
