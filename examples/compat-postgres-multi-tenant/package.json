{
  "name": "compat-postgres-multi-tenant",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant Postgres/Supabase replacement on Durable Objects - drop-in replacement with physical tenant isolation",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "supabase",
    "postgres",
    "multi-tenant",
    "durable-objects",
    "cloudflare-workers",
    "postgrest",
    "rls",
    "row-level-security"
  ],
  "dependencies": {
    "dotdo": "workspace:*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.58.0"
  }
}
