---
title: Installation
description: How to install and set up dotdo
---

# Installation

Get started with dotdo in minutes.

## Prerequisites

Before you begin, ensure you have:

- Node.js 18 or later
- npm or pnpm
- A Cloudflare account (for deployment)

## Quick Start

Install dotdo using npm:

```bash
npm install dotdo
```

Or using pnpm:

```bash
pnpm add dotdo
```

## Development Setup

Clone the repository and install dependencies:

```bash
git clone https://github.com/dot-do/dotdo.git
cd dotdo
npm install
```

## Running Locally

Start the development server:

```bash
npm run dev
```

This will start the Wrangler dev server with hot reloading.

## Testing

Run tests using Vitest:

```bash
npm test              # Watch mode
npm run test:run      # Single run
```

## Building

Build for production:

```bash
npm run build
```

## Deployment

Deploy to Cloudflare Workers:

```bash
npm run deploy
```

## Next Steps

Now that you have dotdo installed, check out:

- [Core Concepts](/docs/core-concepts)
- [API Reference](/docs/api-reference)
- [Examples](/docs/examples)
