{
  "name": "@dotdo/api",
  "version": "0.0.1",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dotdo/auth": "workspace:*",
    "@dotdo/db": "workspace:*",
    "@dotdo/do": "workspace:*",
    "@dotdo/observability": "workspace:*",
    "@dotdo/rpc": "workspace:*",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@dotdo/test-utils": "workspace:*",
    "typescript": "^5.7.0"
  }
}
